-- No longer needed under the new 4-top-level-node tag design.
-- Was auto-promoting any leftover "category" text values into root
-- tags on every restart, re-creating old categories after cleanup.
SELECT 1;
